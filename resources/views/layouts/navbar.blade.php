<header class="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-end h-20">
            <!-- Right Icons -->
            <div class="flex items-center gap-6">
                <!-- User Info -->
                <div class="flex items-center gap-3">
                    <div>
                        <p class="text-sm font-medium">{{ Auth::user()->nama }}</p>
                    </div>
                </div>
            </div>
        </header>